#include <stdio.h>
#include <conio.h>
 void main() {
    int a = 10;

    if (a > 5);  // Semicolon here ends the if statement
    {
        // This block will always execute because of the semicolon above
        printf("This block executes regardless of the if condition.\n");;;;
    }
    ;
    ;

   
   getch();
}